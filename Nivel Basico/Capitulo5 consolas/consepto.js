/*   CAPITULO 4
EN CONSOLA

---funciones de registro
- assert() - aparece un mensaje de error en consola si la afirmacion es falsa, caso contrario no aparese nada (NO ESTANDAR)
console.assert(5<4)
- clear() - limpia la consola 
console.clear()
- error() - muestra un mensaje de error en la consola web
console.error("He capo que hiciste ")
- info() - emite un mensaje informativo a la consola web 
console.info("che bien tu trabajo")
- log() - muestra un mensaje en la consola web (o el interprete javascript)
console.log("Como estas ") // se depura
- table([Arreglo]) - esta funcion toma un agrupamiento obligatorio: data que debe ser un arreglo o un objeto, y un parametro adicional: columns y nos muestra una tabla en consola
console.table([1,2,4,3,2,5,6])
- warn() - imprime un mensaje de advertencia en consola web
console.warn("Che capo, benis bien jej ..")
- dir() - despliega una lista interactiva de las propiedades del objeto de javascript especificado [NO ESTANDAR]
console.dir([2,4,3,6,5,7])

----- Funcion de conteo---
- count() - registra el numero de veses que se llama a count(). esta funcion toma como agrupamiento opcional una etiqueta 
console.count()
- countReset() -  resetea el contador console.count()
console.countReset()

-- Funcion de agrupacion--
- group() - crea un nuebo grupo en linea el el registro de la consola web 
console.group()
- grupEnd() - remueve un grupo en line en el registro de la consola web
console.groupEnd() //elimina el gripo
- groupCollapsed() - grea un grupo en linea pero contraido, el usuario deve expandirlo para verlo
console.groupCollapsed("Frutas")

------funciones de temporizacion--
- time() - inicia un temporizador 
console.time()
- timeEnd() - registra el valor actual de un temporizador
console.timeEnd()
- timeLog() - detiene un temporizador
console.timeLog()

dentroi de h1 
contentEditable = "true"






*/